import React from "react";
import { OktaFooter } from "..";

export default {
  component: OktaFooter,
  title: "OktaFooter",
};

const Template = (args) => <OktaFooter />;

export const Default = Template.bind({});
