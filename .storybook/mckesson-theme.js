import { create } from "@storybook/theming/create";

import brandImage from "./Ontadafull.svg";

export default create({
  base: "light",
  brandTitle: "MCP component library",
  brandImage: brandImage,
  brandTarget: "_self",
});
